# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: sportbuddydb
      POSTGRES_PASSWORD: sportbuddy
#      POSTGRES_HOST_AUTH_METHOD: md5
#      PGDATA: /var/lib/postgresql/data/pgdata

#  adminer:
#    image: adminer
#    restart: always
#    ports:
#      - 4040:4040