spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sportbuddydb #&useSSL=false
    username: sportbuddy_dev
    password: sportbuddy
    platform: postgres

  devtools:
    add-properties: true

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        default_schema: sb_dev
#    USE THIS NEXT STEP FOR DATABASE CREATION
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: sports_database_create.sql